<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
 <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <a class="bg-red-500 rounded-md px-4 py-2" href="/logout">Logout</a>
    <h3 class="mb-5 text-3xl">Profile</h3>
    <h2 class="text-3xl mb-5">Hello, <%= user.name %></h2>

    <form action="/post" method="post">
        <textarea name="content" class="resize-none bg-transparent border-2 border-zinc-800 rounded-md w-1/3 p-3 mb-2" placeholder="What's on your mind?"></textarea>
        <input class="px-3 py-2 bg-blue-500 rounded-md mt-2 w-42 margin-left" type="submit" value="Create new post">
    </form>

    <div class="posts mt-20">
        <h3 class="text-zinc-400">Your posts</h3>
        <div class="post-container mt-5 flex flex-col gap-4">
            <% user.posts.reverse().forEach(function(post) { %>
                <div class="bg-zinc-800 p-4 rounded-md">
                    <p class="text-lg"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%=post.likes.length %></small>
                    <a class="text-blue-500 gap-4" href="/like/<%=post._id %>">Like</a>
                    <a class="text-zinc-500" href="/like/<%=post._id %>">edit</a>
                </div>
            <% }) %>
        </div>
    </div>
 </div>   
</body>
</html>
